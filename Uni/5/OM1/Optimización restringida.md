
# Optimización restringida

- Se puede pasar de un problema restringido a uno no restringido mediante **Lagrange**. 

Vamos a tener una nueva función que va a ser:

$$F(x) = f(x) + r_1*g_1(x) + r_2*g_2(x)$$
Donde $r_1, r_2 > 0$  

Sabemos que hemos encontrada una solución cuando:

$$-f'(x_1, x_2) = r_1*g'_1(x_1, x_2) + r_2*g'_2(x_1, x_2)$$

Para restricciónes de desigualdad.